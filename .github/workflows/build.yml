# Build on both platforms that matter: macOS (development, Resolume-adjacent
# testing) and Windows x64 (the vMix target; runs under emulation on Windows
# on ARM too). No release packaging yet — this exists to keep the Windows
# port honest from the first commit that has one.

name: build

on:
  push:
    branches: [main]
  pull_request:
  # Manual trigger. Added 2026-08-06 because GitHub stopped creating runs for
  # this repo's pushes — workflows active, YAML valid, triggers correct, pushes
  # registered, and every other repo in the fleet building normally — so there
  # was no way to ask for a build at all. Touching the workflow file is also the
  # usual way to make GitHub re-register a workflow that has got stuck.
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            name: macos
          - os: windows-latest
            name: windows-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Smoke test (no plugin, no network)
        shell: bash
        run: |
          BIN=build/oxbow
          [ -f build/Release/oxbow.exe ] && BIN=build/Release/oxbow.exe
          # Usage text on no args; exit code 2 by design.
          "$BIN" || [ $? -eq 2 ]

      - uses: actions/upload-artifact@v4
        with:
          name: oxbow-${{ matrix.name }}
          path: |
            build/oxbow
            build/Release/oxbow.exe
          if-no-files-found: error
